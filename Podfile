# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ABPortfolio' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for ABPortfolio
  pod 'ABMediaView'
  pod 'ABUtils'
  pod 'ABKeyboardAccessory'
  pod 'ABVolumeControl'

  target 'ABPortfolioTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ABPortfolioUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
